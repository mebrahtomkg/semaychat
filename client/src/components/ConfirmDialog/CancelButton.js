

import styled from 'styled-components';
import ActionButton from './ActionButton';

const CancelButton = styled(ActionButton)`
  background: #173a71;
  border: 1px solid #303e8b;
`;

export default CancelButton;
